# running-paths_generator

Generates multiple paths around a choosen starting point.

Parameters can be changed inside the `parameters.yaml` config file (contains relevant documentation) and/or using the main script's arguments (`--help` flag for help).

More information on the project structure in `README.md`s inside the respective folders.
